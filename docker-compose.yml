services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-frx}
      POSTGRES_USER: ${POSTGRES_USER:-frx}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-frx-change-me}
    volumes:
      - frx_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 20

  app:
    build: .
    image: frx-portal:latest
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "${PORT:-8080}:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: "3000"
      POSTGRES_DB: ${POSTGRES_DB:-frx}
      POSTGRES_USER: ${POSTGRES_USER:-frx}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-frx-change-me}
      DATABASE_URL: postgres://${POSTGRES_USER:-frx}:${POSTGRES_PASSWORD:-frx-change-me}@db:5432/${POSTGRES_DB:-frx}

volumes:
  frx_pgdata:
