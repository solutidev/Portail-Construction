# Copy to .env on the server:
#   cp .env.sample .env
# Then edit POSTGRES_PASSWORD. Keep the same password in DATABASE_URL.

# Public port on the host (container listens on 3000)
PORT=8080

# Git branch used by scripts/update.sh
GIT_BRANCH=main

# Local Docker Postgres (service name is `db`)
POSTGRES_DB=frx
POSTGRES_USER=frx
POSTGRES_PASSWORD=frx-change-me
DATABASE_URL=postgres://frx:frx-change-me@db:5432/frx

# Optional. Can also be set later in Setup → SharePoint
SHAREPOINT_CLIENT_SECRET=
